```python
def func(arr, d):
    k = arr.index(d)
    newList = []
    newList = arr[k+1:]+arr[0:k+1]
    return newList
 
arr = [1, 2, 3, 4, 5, 6, 7]
d = 2

arr = func(arr, d)
for i in arr:
    print(i, end=" ")
```
